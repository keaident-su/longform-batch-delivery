---
name: longform-batch-delivery
description: |
  长文本分批交付协议。用于任何"一次性写不完"的超长产出（十几万字的小说/剧本/报告/多卷文档）。
  解决的核心痛点：模型单轮输出有硬上限，一次性承诺 17 万字必然被截断，最后用户才发现"只完成 30%"。
  本技能强制：开工先测算体量→写死分批计划→建持久化进度账本→每批落盘源文件→每批重建+校验+汇报→
  用户只说"继续"即可断点续写，任何时刻都能看到"目标/当前/还差"。
  触发场景：目标字数 > 单轮安全产出（约 10,000 中文字符）；需要分卷/分册交付；需要跨多轮续写且不能丢进度；
  用户抱怨"又没写够""每次都要重新说一遍要求""写完才发现差一大截"。
  English: A batching/delivery protocol for ultra-long outputs (100k+ Chinese chars, multi-volume docs).
  Prevents the "single-turn output cap" failure: measure volume first, fix a batch plan, keep a persistent
  progress ledger, dump each batch to source files, rebuild+verify+report every batch, and resume on "continue".
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
metadata:
  version: 1.0.0
  lang: zh-CN, en
  license: MIT
---

# 长文本分批交付协议（LFBD）

## 0. 这个技能解决什么

模型单轮回复有**输出上限**。想在一轮里吐出 170,000 中文字符，物理上做不到——结果就是被截断，
或者只写了三成却到最后才告诉用户。用户的体验是：**写了几轮，才发现差一大截，还得从头解释一遍。**

本技能把这件事从"坑"变成"流程"：**先算量、再分批、有账本、可续写、每批可见进度。**

---

## 1. 三条铁律（违反即失败）

1. **不许在第一轮承诺"一次性写完"。** 收到超长任务后，第一轮只做三件事：测算、写死分批计划、把计划告诉用户。
2. **正文只落盘，不进聊天。** 每一批的产物写成源文件（`src/`、`scenes/`、`chapters/`…），聊天里只报"进展表 + 校验结果"。
3. **每批结束必须重建 + 校验 + 汇报一次。** 汇报格式固定，永远包含 `目标 / 当前 / 还差`。

---

## 2. 开工第一步：体量测算（必做）

```
单轮安全产出 P ≈ 8,000 ~ 10,000 中文字符   （保守值，含四字段/标题等冗余）
批次数 N = ceil(目标字数 / P)
```

把 N 直接告诉用户，并给出**分批计划表**：

| 批次 | 范围（场/章） | 计划字数 | 累计 |
|---|---|---|---|
| 1 | 第1—40场 | 10,000 | 10,000 |
| 2 | 第41—80场 | 10,000 | 20,000 |
| … | … | … | … |

> 例：目标 170,000 字 → N ≈ 17–21 批。**明确说"这需要 N 轮"，而不是"我尽量一次写完"。**

**红线表**

| 目标体量 | 必须分批 | 说明 |
|---|---|---|
| < 10,000 字 | 否 | 单轮可完成 |
| 10,000—30,000 字 | 是，2—4 批 | 单轮必被截断 |
| 30,000—100,000 字 | 是，4—12 批 | 必须建账本 |
| > 100,000 字 | 是，12 批以上 | 必须建账本 + 骨架先行 |

---

## 3. 骨架先行（Skeleton First）

大批量写作**不要边想边写**。顺序是：

1. 先产出**全量骨架**：每个场次/章节只写"编号 + 一句锚点 + 计划字数"，不写正文。
2. 骨架一次交付给用户确认（成本低、可改）。
3. 之后每一批只做一件事：**把骨架里的一段填成正文**。
4. 填充时**不得改动骨架的编号与顺序**（编号是续写的锚）。

好处：编号连续性、时间线、结构在第一批就锁死，后面不会返工。

---

## 4. 持久化进度账本（Ledger）

在项目根放 `LEDGER.md` 和 `ledger.json`。**每批结束必须更新**，这是断点续写的唯一依据。

`ledger.json` 结构：

```json
{
  "target": 170000,
  "unit": "cjk_chars",
  "per_batch_budget": 10000,
  "batches_planned": 18,
  "batches_done": 3,
  "current_total": 31500,
  "cursor": { "chapter": 9, "scene": 242 },
  "updated_at": "2026-09-14T16:50:00+08:00",
  "batches": [
    {"n": 1, "range": "第171-200场", "added": 10400, "total": 10400, "verify": "PASS"},
    {"n": 2, "range": "第201-230场", "added": 10100, "total": 20500, "verify": "PASS"},
    {"n": 3, "range": "第231-242场", "added": 11000, "total": 31500, "verify": "PASS"}
  ],
  "checks": {
    "id_continuous": true,
    "time_monotonic": true,
    "fields_complete": true,
    "degradation": "clean"
  }
}
```

`LEDGER.md` 是人读版（进展表 + 还差多少 + 下一批打算写什么）。

---

## 5. 单批标准作业流程（SOP）

```
1. 读 LEDGER.md → 确认 cursor 与下一批范围
2. 只写这一批（目标 8,000–10,000 中文字符，宁少勿超）
3. 落盘为源文件（文件名带序号，便于排序）
4. 运行 build_and_verify.py
5. 更新 ledger.json / LEDGER.md
6. 汇报（格式见 §7）
7. 停。等用户说"继续"
```

**不要**在同一批里顺手改已经验收过的历史章节——那会让校验基线失效。

---

## 6. 验收闸门（每批必跑，全过才算完成）

| 闸门 | 判据 | 失败处理 |
|---|---|---|
| G1 字数 | `current_total` 单调增长，且 ≥ 本批承诺 | 立刻补写，不许把差额留到下一批 |
| G2 编号连续 | 主编号（场/章）无跳号、无重号 | 重排 + 全局回写 |
| G3 时间单调 | 时间戳严格递增，零倒流 | 调整时间戳 |
| G4 字段齐全 | 每单元的必填字段全在（如：锚点/时间/地点/人物） | 补齐 |
| G5 结构完整 | 集/卷/章标题齐全，无丢失 | 补标 |
| G6 退化扫描 | 无"主语，动词"式碎片断句、无空行注水、无高重复度 | 改写 |
| G7 落盘可打开 | 交付文件（docx/pdf…）能被目标软件打开 | 重生成 |

> G6 的"疑似碎片断句"是**启发式**提示（会命中"他熄火，锁车。"这类合法的短句节奏），
> 只告警、不判失败；连续空行 ≥3 才判失败。真正要人人过一遍的是 G1、G2、G3、G4、G5。

**G1 是红线**：任何时候用户问"还差多少"，你都必须秒答，不许现算。

---

## 7. 汇报格式（固定，不许改）

```
## 第 N 批已交付
| | 上轮 | 本轮 | 增量 |
|---|---|---|---|
| 上册 | … | … | … |
| 下册 | … | … | … |
| 合计 | … | … | … |

目标 X ｜ 当前 Y ｜ 还差 Z（完成度 P%）

新增：<一段话说清这批写了什么>
校验：G1✓ G2✓ G3✓ G4✓ G5✓ G6✓ G7✓

下一批：<范围 + 计划字数>
```

这一张表，就是让用户**永远不必自己发现"又不够"**。

---

## 8. 断点续写（Resume）

用户只说"继续"时：

1. 读 `LEDGER.md` / `ledger.json`；
2. 取 `cursor`，算出下一批范围；
3. 按 §5 执行；
4. **不要重新问需求**，不要重述背景——需求已经写在骨架和账本里。

会话可能被压缩或换新会话，所以**账本必须是磁盘上的文件**，不能只存在于对话里。

---

## 9. 反模式（见到就停手）

- ❌ 先答应"我一次写完"，然后被截断
- ❌ 把几万字正文直接贴在聊天里（既进不了上下文，也丢内容）
- ❌ 用"补充场/附录"堆字数而主线不推进
- ❌ 跳过某一批的校验，凑到最后一起查
- ❌ 进度只存在对话里，换会话就归零
- ❌ 目标字数差一大截却先汇报"已完成"，把差距留到最后

---

## 10. 配套脚本

- `scripts/ledger.py`：账本的初始化/更新/查询/算下一批（无第三方依赖）。
- `scripts/build_and_verify.py`：通用"重建 + 校验"模板，扫描源目录 → 排序 → 校验 G2–G6 → 输出字数与体检报告。
  默认支持"`第N场` / `第N章` + 补充场"这类结构，字段名可在 `CONFIG` 里改。
- `templates/`：账本模板与分批计划模板。

用法见 `docs/workflow.zh.md` 与 `docs/workflow.en.md`。
