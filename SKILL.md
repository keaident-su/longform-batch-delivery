---
name: longform-batch-delivery
description: |
  长文本分批交付协议（LFBD v7）。用于任何"一次性写不完"的超长产出（十几万字的小说/剧本/报告/多卷文档）。
  v7 的核心修复：把"连续跑批"变成默认行为——一轮内必须连续提交多批直到输出上限，绝不在中途停下问"要继续吗"；
  并把"完成"重新定义为一个可判定的式子：done = (字数达标) AND (G1–G10 全部通过)。不到达标就是"未完成"，不许汇报成"已完成"。
  English: A batching/delivery protocol for ultra-long outputs (100k+ chars). v7 makes continuous batching the default
  and defines completion as a decidable predicate: done = (word count reached) AND (all gates pass).
metadata:
  version: 7.0.0
  trigger: 目标字数远超单轮输出上限；需要分卷/分批交付；跨多轮续写且不能丢进度；用户抱怨"又没写够""每次都要重新说要求"
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
  - AskUserQuestion
---

# 长文本分批交付协议 LFBD v7

> 一句话：**先测产出、再排期；一轮内连续跑批；每批都有账本；差多少永远秒答；达标即停，不达标绝不停。**

---

## 0. 为什么"用了技能还是没写完"

v6 收到最多的抱怨是同一句：**"技能装上了，可它还是写一点就停下来问我。"**

根因不是技能没写清楚，是 v6 只描述了"应该怎么做"，没有把**"停下"定义成违规**。v7 把这一条改成硬约束。

| 编号 | 真实约束 | v6 的病 | v7 的药 |
|---|---|---|---|
| **R1** | 单轮输出有硬上限（约 6,000–12,000 中文字符），这是平台的物理限制，提示词改不掉 | 说明里提了，但没有强制行为 | 把"一轮内连续跑批"写成**默认动作**，把"只写一批就停下问用户"列为**头号反模式** |
| **R2** | 真实吞吐必须实测，不是估的；实测常见 **300–600 字/块** | 用"每轮一万字"估值排期 | 第 1 批结束立刻标定 `字/块`、`字/轮`，并用实测值重排 |
| **R3** | 用短"补充单元"堆量会做成**空心稿** | 只统计总字数，不检查单块体量 | **单元地板**＋未达标清单＋"加长单块，不许加块数" |
| **R4** | 多轮/并行会把同一编号写两遍 | 无写者约束 | **单写者锁**＋**构建前重号即失败** |
| **R5** | "完成"是个模糊词，模型天然倾向于说"已完成" | 无判定式 | **done = (current ≥ target) AND (G1–G10 全过)**；未达标一律称"未完成" |

记住 R1：**只要 目标字数 ÷ 单轮上限 > 1，就不可能"一轮写完"。** 这不是能力问题，是算数问题。
记住 R5：**没达标就叫没完成。** 不许把"交付了第一批"说成"写完了"。

---

## 1. 六条铁律（违反即失败）

1. **不许承诺"一轮写完"**；但**必须承诺"一轮内连续跑批到上限"**。第一轮先做三件事：测算、写死分批计划、把计划贴出来。
2. **正文只落盘，不进聊天。** 每批产物写成源文件（`src/`、`scenes/`、`chapters/`），聊天里只报进度表。
3. **每批结束必须重建＋校验＋更新账本**，汇报永远含 `目标 / 当前 / 还差 / 完成度`。
4. **连续模式（新默认）**：一旦用户说过"全部写完""不许停""连续处理""一直跑到达标"——在同一轮里**连续跑批**（落盘→校验→更新账本→再落盘→再校验），直到本轮输出上限；**不许中途停下来问"要继续吗"**。到了上限，只在末尾报一次进度并给出"下一轮第一件事"，然后本轮结束。
5. **达标才停**：`done` 成立后方可宣布完成；`done` 不成立时，任何"完成""交付完毕"的措辞都是违规。
6. **同一编号不得出现在两个文件里**；发现即失败，人工裁决，**绝不自动合并**。

---

## 2. 体量测算与标定

```
单轮安全产出 P0 ≈ 9,000 中文字符（保守初值）
预估批次数 N0 = ceil(目标 / P0)
```

**第 1 批跑完立刻标定（关键）：**

```
实测字/块  = 本批新增字数 ÷ 本批新增块数
实测字/轮  = 本批新增字数
剩余轮数   = ceil(还差字数 ÷ 实测字/轮)
```

**判断规则（v7）**：

- 若 `实测字/块 < 800`：**不要加快写块数，要加长每块**。否则你会写出几百个碎片块，既难看又不推进主线。
- 若 `实测字/轮 < 6,000`：说明你在"分批"的名义下偷懒，一轮本可以跑更多批。回看第 4 条铁律。

---

## 3. 骨架先行（Skeleton First）＋ 单写者锁

顺序不能反：

1. 先产出**全量骨架**：每个单元只写"编号 + 一句锚点 + 计划字数"，不写正文。
2. 骨架一次交付确认（成本低、可改）。
3. 之后每批只做一件事：把骨架里的一段填成正文。
4. 填充时**不得改动骨架编号与顺序**（编号是续写的锚）。

**单写者锁**：项目根写 `RUN.lock`：

```json
{ "run_id": "2026-09-14T18:20", "file_prefixes": ["act4_"], "owner": "session-A" }
```

- 构建脚本**只扫描 `file_prefixes` 里的文件**，其余同前缀文件视为污染，直接报错；
- 同编号出现在不同文件里 → **先失败，再人工决定留哪一份**。

---

## 4. 单元字数地板（反注水核心）

| 单元 | 地板（中文字符） |
|---|---|
| 剧本·主场次 | ≥ 700 |
| 剧本·补充场 | ≥ 500（v7 建议补写块 ≥ 1,200） |
| 小说·章 | ≥ 2,000 |
| 报告·节 | ≥ 800 |

构建脚本对每个单元检查地板；**低于地板的单元列入"未达标清单"**，并算出：

```
需补字数 = Σ(地板 − 实际字数)
需补单元数 ≈ ceil(需补字数 ÷ 平均单元字数)
```

打印出**可以直接照着写的下一批清单**（`WORKLIST.md`）。这一步把"还差多少"从一句口号变成一张作业单。

---

## 5. 持久化账本

项目根放 `LEDGER.md`（人读）与 `ledger.json`（机读），**每批结束必须更新**。`ledger.json`：

```json
{
  "target": 170000, "unit": "cjk_chars",
  "calibrated": { "chars_per_block": 620, "chars_per_round": 9200 },
  "current_total": 106580, "remaining": 63420, "progress_pct": 62.7,
  "cursor": { "chapter": 12, "scene": 270 },
  "floors": { "main": 700, "supp": 500 },
  "done": false,
  "checks": { "G1": true, "G2": true, "G3": true, "G4": true, "G5": true, "G6": true, "G7": true, "G8": true, "G9": false, "G10": false }
}
```

`LEDGER.md` 必含四行：目标、当前、还差、完成度，以及"下一批范围 + 计划字数"。

**`done` 字段是唯一权威**：`done = (current_total >= target) && G1..G10 全过`。

---

## 6. 单批标准作业流程（SOP）

```
1. 读 LEDGER.md / ledger.json → 取 cursor 与下一批范围
2. 只写这一批（目标 = 实测字/轮，宁少勿超）
3. 落盘（文件名带序号，遵循 RUN.lock 前缀）
4. 运行 scripts/build_and_verify.py
5. 字数不够 → 立刻在本批内补写，不许把差额留到下一批
6. 更新 ledger.json / LEDGER.md（含 done 字段）
7. 汇报（格式见 §8）
8. 连续模式：回到步骤 1，继续下一批，直到本轮输出上限或 done=true
```

**不要**在同一批里回头改已验收的历史单元——那会让校验基线失效。

---

## 7. 验收闸门（每批必跑）

| 闸门 | 判据 | 失败处理 |
|---|---|---|
| G1 字数 | `current_total` 单调增长且 ≥ 本批承诺 | 立刻补写 |
| G2 编号连续 | 主编号无跳号、无重号 | 重排＋全局回写 |
| G3 时间单调 | 时间戳严格递增，零倒流 | 用 `slot_allocator.py` 重排 |
| G4 字段齐全 | 每单元必填字段全在 | 补齐 |
| G5 结构完整 | 集/卷/章标题齐全 | 补标 |
| G6 退化扫描 | 无"主语，动词"碎片、无连续空行 ≥3 | 改写 |
| G7 交付可打开 | docx/pdf 能打开 | 重生成 |
| G8 重号污染 | 同编号不出现在两个文件；前缀外文件不得被扫描 | **失败即停，人工裁决** |
| G9 单元地板 | 每单元字数 ≥ 地板 | 输出补写清单 |
| **G10 达标判定** | `done = (current ≥ target) && G1–G9 全过` | 未达标 → 继续跑批，**不得宣布完成** |

---

## 8. 汇报格式（固定）

```
## 第 N 批已交付
| | 上轮 | 本轮 | 增量 |
|---|---|---|---|
| 上册 | … | … | … |
| 下册 | … | … | … |
| 合计 | … | … | … |

目标 X ｜ 当前 Y ｜ 还差 Z（完成度 P%）｜ done = false
实测吞吐：字/块 = a ｜ 字/轮 = b ｜ 预计还需 r 轮

新增：<一段话说清这批写了什么>
校验：G1✓ G2✓ … G10✗（未达标，继续）

下一批：<范围 + 计划字数>
```

这张表让用户**永远不必自己发现"又不够"**。

---

## 9. 断点续写（Resume）

用户只说"继续"时：

1. 读 `LEDGER.md` / `ledger.json`；
2. 若 `done = true`，直接告知已完成，不再写；
3. 否则取 `cursor`，用**实测吞吐**算下一批范围与轮数；
4. 按 §6 执行（处在连续模式时继续链式跑批）；
5. **不要重新问需求**，不要重述背景。

---

## 10. 反模式（见到就停手）

- ❌ **只写一批就停下来问"要继续吗"**（v7 头号反模式）
- ❌ 把"交付了第一批"说成"写完了"
- ❌ 先答应"一次写完"，然后被截断
- ❌ 把几万字正文贴在聊天里
- ❌ 用短"补充场/附录"堆字数而主线不推进
- ❌ 跳过某批校验，凑到最后一起查
- ❌ 进度只存在对话里，换会话就归零
- ❌ 明明差一大截却先汇报"已完成"
- ❌ 用"每轮 10,000 字"的估值排期，从不回测
- ❌ 多轮/并行运行共用同一目录却不加锁

---

## 11. 配套脚本

| 脚本 | 作用 |
|---|---|
| `scripts/ledger.py` | 账本初始化/更新/查询/算下一批 |
| `scripts/build_and_verify.py` | 重建＋G2–G10 全部校验＋输出**下一批作业单** |
| `scripts/size_report.py` | 一行命令回答"目标/当前/还差/完成度/还需几轮/done" |
| `scripts/slot_allocator.py` | 给新补充单元分配**保证单调递增**的时间戳 |
| `scripts/publish_github.py` | 把技能目录整体推送到 GitHub 仓库（UTF-8 安全） |

用法见 `docs/workflow.zh.md` / `docs/workflow.md`。

---

## 12. 排障表（症状 → 原因 → 动作）

| 症状 | 原因 | 动作 |
|---|---|---|
| "写了几轮，发现只完成 30%" | 用估值排期，未标定 | 跑 `size_report.py`，用实测吞吐重排 |
| "每轮都只写一点就停" | 未进入连续模式 / 怕超限 | 回 §1 第 4 条：一轮内连续跑批到上限 |
| "块数很多但总字数上不去" | 单块太短 | 看 G9 未达标清单，**加长单块**而不是加块数 |
| "两轮之间编号对不上/出现重复场次" | 多写者/多轮共用目录 | 建 `RUN.lock`；G8 失败即停，人工裁决 |
| "补写单元时间戳倒流" | 未查同场景既有补单元的最后一个时间 | 用 `slot_allocator.py` 取安全窗口 |
| "换了个会话就全部重来" | 进度只在对话里 | 账本必须落盘（§5） |
| "每轮都要重新说一遍要求" | 需求没写进骨架 | 骨架先行（§3），需求进骨架不进对话 |
| "最后才发现差一大截" | 没有固定汇报表 | 用 §8 格式，每批强制 `还差 Z` |

---

## 13. 参考

基于 [Wikipedia:Signs of AI writing](https://en.wikipedia.org/wiki/Wikipedia:Signs_of_AI_writing) 的思路，本协议关注的是**工程可控性**而非文风。
