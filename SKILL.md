---
name: longform-batch-delivery
version: 9.1.0
description: |
  长文本分批交付协议（LFBD）。用于任何"一次性写不完"的超长产出（十几万字的小说/剧本/报告/多卷文档）。
  v9 核心：先把"要跑几轮"算出来，再把"完成"变成退出码——
  轮数契约 x = 目标字数 ÷（单轮输出上限 × 0.8）；先跑满 x 轮，
  再用多种口径核字数（源文件汉字／含全角标点／去空白／交付 docx），不够就补；
  够了才进入排查，问题全过才允许停。
  scripts/run_state.py 四阶段：GENERATE / AUDIT / REPAIR / DONE；
  gate 返回 0=DONE／1=继续写／2=先修问题，非 0 一律不许停、不许提问。
  配套轮末契约（每轮只许以 DONE 或 RESUME 结尾）与吞吐升级梯（逼单块变长而非块数变多）。
  并把 N 次接续压成 N/k 次：一个回合内链式连跑 k 块，块与块之间不收尾。
  触发场景：目标字数远超单轮上限；分卷/分册交付；跨多轮续写不丢进度；
  用户抱怨"又没写够""每次都要重新说一遍要求""写完才发现差一大截"。
  English: LFBD — batching/delivery for ultra-long outputs (100k+ CJK chars).
  v9 pre-computes the round count x = target ÷ (per-turn cap × 0.8), then loops
  through four phases: GENERATE / AUDIT / REPAIR / DONE. Volume is checked by
  several methods (CJK, CJK+punctuation, non-space, built docx); if short, keep
  writing; once met, run all gates; only if everything passes may you stop.
  gate returns 0=DONE / 1=write more / 2=fix first — never end a turn with a question.
  Chaining k chunks inside one turn (no wrap-up between chunks) cuts hand-offs to about 1/k.
allowed-tools:
  - Read
  - Write
  - Edit
  - Bash
---

# 长文本分批交付协议 LFBD v9

> 一句话：**先测产出、再排期；每轮都有账本；差多少永远秒答；不许用短段落注水。**
> **v8 一句话："完成"不是你说了算，是退出码说了算（`gate` exit 0）。**
> **v9 一句话：先算出"要跑几轮"（x = 目标 ÷ 上限×0.8），再按 GENERATE→AUDIT→REPAIR→DONE 转，非 DONE 不停。**

---

## 0. 先说清楚：为什么"用了技能还是没写完"

这是 v5 收到最多的抱怨。根因不在"技能没生效"，而在下面四个真实约束：

| 编号 | 真实约束 | v5 的病 | v6 的药 |
|---|---|---|---|
| **R1** | 单轮输出有硬上限（约 8,000–12,000 中文字符），这是模型/平台的物理限制，任何提示词都改不掉 | 用户以为"分批＝多轮但每轮自己会接着写" | 明确宣布轮数；用户说"全部写完"时，**一轮内连续提交多批**直到上限，不问"继续吗" |
| **R2** | **真实吞吐必须实测**，不是估的。实测中常见：单块只有 300–600 字，而不是你以为的 1,500 字 | v5 用"每轮 10,000 字"估算，排期严重乐观 | 第 1 批结束**立刻标定**：`字/块`、`字/轮`，然后用实测值重排剩余轮数 |
| **R3** | 用"补充场/附录"堆量会做出**空心稿**：块数很多、字数很少、主线没走 | v5 只统计总字数，不检查单块体量 | **单元字数地板**：每个场/章低于地板就判失败，并自动生成"还差几个达标单元"的补写清单 |
| **R4** | 多轮/并行运行会把同一编号写两遍（本次实测出现过：同一批场次出现两套文件，编号 171–270 重复） | v5 无写者约束、无重号检测 | **单写者锁**（运行前缀清单）+ 构建前**重号即失败** |

记住 R1。**只要目标字数 ÷ 单轮上限 > 1，就不可能"一轮写完"。** 这不是能力问题，是算数问题。

### 0.1 v7 的病：闸门全过，却照样提前收工

v6/v7 一共十个闸门，全都在回答"这份稿子写得好不好"。
**没有一个闸门在回答"现在允许停吗"。**

于是真实现场（本项目 2026-09 事故）：

| 现象 | 根因 | v8 的药 |
|---|---|---|
| 目标 170,000，写到 27,822（16.4%）就"交付" | **"完成"是模型的主观判断**，没有任何东西能否决它 | M1 停止谓词：`gate` 退出码 |
| 结尾附"要不要先出上册，回我一句" | 轮末行为没有规定 | M2 轮末契约：只许 `✅ DONE` / `⏩ RESUME` |
| 实测 397 字/块，下一轮照旧写短块 | 只"报告"了吞吐，没有"强制"加长 | M3 吞吐升级梯 |
| 用户必须每轮手动说"继续" | §9 断点续写把责任推给了用户 | `resume` 一行指令 + 状态机自动接着跑 |

一句话：**v7 教你把它写好，v8 逼你把它写完。**

---

## 1. 四条铁律（违反即失败）

1. **不许承诺"一轮写完"。** 收到超长任务后，第一轮只做三件事：测算、写死分批计划、把计划告诉用户。
2. **正文只落盘，不进聊天。** 每批产物写成源文件（`src/`、`scenes/`、`chapters/`），聊天里只报进度表。
3. **每批结束必须重建＋校验＋汇报一次**，汇报永远含 `目标 / 当前 / 还差 / 完成度`。
4. **用户说"全部写完""不许停""不要每轮问我"时**：在同一条回复里**连续跑批**（落盘→校验→再落盘→再校验），直到本轮输出上限；**不许中途停下来问"要继续吗"**，只在末尾报一次进度。
5. **"完成"必须由退出码证明。** 收工前必须跑 `python scripts/run_state.py gate`，**退出码非 0 一律不许写"完成"、不许收尾**。模型的自评无效。
6. **轮末只许两种结尾。** `✅ DONE`（仅当 gate exit 0）或 `⏩ RESUME`（其余一切情况）。**禁止任何提问、征询、提议**——包括"要不要…""如果你希望…""回我一句…"。
7. **先算轮数再开写。** `x = ceil(目标 ÷ (单轮上限 × 0.8))`。但**跑满 x 轮 ≠ 完成**，验收只看 `gate`。
8. **块与块之间不得收尾。** 一个回合里链式跑满 `chunks_per_turn` 块（默认 3）；写完一块要立刻写下一块，**不许在块之间输出汇报或提问**。把"需接续 N 次"压成"N ÷ k 次"。

---

## 1.5 停止谓词与轮末契约（v8 引入，v9 沿用）

### 1.5.1 三条机制

| 机制 | 名字 | 作用 |
|---|---|---|
| **M1** | **停止谓词（Stop Predicate）** | `run_state.py gate` 的退出码是唯一的"完成"判据；模型自评一律无效 |
| **M2** | **轮末契约（Turn-end Contract）** | 每轮回复只许以 `✅ DONE` 或 `⏩ RESUME` 结尾；禁止任何提问/征询/提议 |
| **M3** | **吞吐升级梯（Escalation Ladder）** | 用算术逼单块变长，而不是块数变多 |

### 1.5.2 M1：`gate` 的三个退出码

```bash
python scripts/run_state.py gate
```

| 退出码 | 状态 | 含义 | 允许做的事 |
|---|---|---|---|
| **0** | `DONE` | 字数 ≥ 目标 **且** 全部闸门通过 **且** 交付物存在 | **只有此时**才可以在回复里写"完成" |
| **1** | `GENERATE` | 字数未达标 | 立刻接着写下一批。不许汇报"已完成"，不许提问 |
| **2** | `REPAIR` | 字数达标，但有形式问题（编号/时间/字段/退化/地板） | 按清单修，不写新内容，修完重跑 |
| **2** | `BLOCKED` | 重号 / G10 冲突 / 交付物损坏 | **先处理，再重跑**，不是停下来问 |

铁令：**写完一批 → 跑 `gate` → 看退出码。退出码不是 0，这一轮就没有结束。**

### 1.5.3 M2：轮末契约（逐条照做，不许发挥）

每一条回复，结尾**必须**恰好是下面两者之一：

```
✅ DONE — gate exit 0
目标 X ｜ 当前 Y（≥X）｜ 全部闸门通过 ｜ 交付物：<文件名>
```

或

```
⏩ RESUME
<run_state.py resume 打印的那一行原文>
```

**绝对禁止**在结尾出现（v7 就是这样被绕过去的）：

- ❌ "要不要我先出上册？" / "需要我继续吗？"
- ❌ "如果你想……回我一句'继续'"
- ❌ "本轮到上限，下一轮我再接着写"（除非**紧跟** `⏩ RESUME` 块）
- ❌ 任何以问号结尾、等待用户输入的句子

> 判定口诀：**结尾没有 `✅ DONE` 或 `⏩ RESUME`，这一轮就是违规的。**

### 1.5.4 M3：吞吐升级梯（治"块多字少"）

v7 只做到"测出 397 字/块"然后写一句"我会把每场显著加长"——那是愿望，不是规则。
v8 把它变成算术：

```
本轮每单元目标 cpu = max(主单元地板 × 1.4, 上一轮实测 cpu × 1.35)，上限 cpu_max
本轮单元数 units   = 单轮预算 turn_budget ÷ cpu
```

即：**吞吐越低，下一轮单元数越少、每单元越长**。写不满 `cpu` 的单元直接判不合格。
`tick --added X --units N` 每次自动执行这条升级梯；连续两轮吞吐不升会打印 STALL 警告并继续抬高。

### 1.5.5 一轮的标准动作（照抄）

```
1. python scripts/run_state.py plan     → 拿作业单（单元数 × 每单元字数）
2. 按作业单写，落盘（只写源文件，正文不进聊天）
3. python scripts/run_state.py tick --added <新增字数> --units <单元数> --cursor <最新编号>
4. python scripts/run_state.py gate
5. 退出码 0 → 结尾写 ✅ DONE；否则 → 回到第 1 步继续，结尾写 ⏩ RESUME
```

**换会话也不需要用户做任何事**：读 `run_state.json` → 跑 `plan` → 接着写。用户唯一需要说的是"继续"。

---

## 1.6 轮数契约与四阶段循环（v9 核心：把"跑几轮"先算出来）

### 1.6.1 轮数契约 x

```bash
python scripts/run_state.py init --target 170000 --cap 12000 --util 0.8
```

```
轮数 x = ceil(目标字数 ÷ (单轮输出上限 × 利用率))
       = ceil(170000 ÷ (12000 × 0.8))
       = ceil(170000 ÷ 9600) = 18 轮
```

`--cap` 是**模型单轮输出上限**（中文字符，默认 12000），`--util` 默认 0.8。
x 一旦算出就写进 `run_state.json` 的 `rounds_planned`，此后每轮都显示 `轮 n/x`。

### 1.6.2 六个步骤（与需求口径一一对应）

| 步骤 | 需求口径 | 机器实现 |
|---|---|---|
| ① | 设轮数 x = 总字数 ÷（上限×0.8） | `init` 打印并写入 `rounds_planned` |
| ② | 先跑满 x 轮 | `plan` 给作业单，`tick` 记账为 `轮 n/x` |
| ③ | 用不同方式查字数 | `count` 同时给出汉字／含全角标点／去空白／总字符／**交付 docx** 五种口径 |
| ④ | 不够就继续补字数 | `gate` exit 1（GENERATE）→ 回到 ② |
| ⑤ | 够了再排查问题 | `gate` 自动进入 AUDIT：编号/时间/字段/退化/地板/G10 |
| ⑥ | 排查通过才停 | `gate` exit 0（DONE）；否则 exit 2（REPAIR/BLOCKED）→ 修完重跑 |

> 注意：**跑满 x 轮不等于完成**。x 只是排期；真正的停止条件永远是 `gate` exit 0。

### 1.6.3 “最严口径”原则

`gate` 判定字数时取**最严的那个口径**：配置了交付物时，取 `min(源文件汉字数, docx 汉字数)`。
这样出现“源文件够、生成的 docx 却漏了内容”时，不会被误判成达标。
`count` 会额外报告 docx 与源文件的偏差，偏差 > 2% 直接预警。

### 1.6.4 一轮的命令序列

```bash
python scripts/run_state.py next     # 看当前阶段（一句话 + 退出码）
python scripts/run_state.py plan     # 作业单：轮次 n/x、单元数、每单元最少多少字
python scripts/run_state.py count    # 多路核字数
python scripts/run_state.py tick --added 9200 --units 5 --cursor 214
python scripts/run_state.py gate     # 唯一合法的停止判据
```

---

## 1.7 轮内链式续跑（v9.1：把接续次数砍掉一截）

### 原理

一个回合（turn）并不是"一次模型输出"。只要中间夹着工具调用，同一回合里可以发生**多次**模型输出。
证据：你看到的这一轮对话里，助手的单次回复包含了十几次工具调用——回合是等助手
**输出一条不带工具调用的消息**才结束的。

所以真正卡住“17 万字要催 18 次”的，不是输出上限，而是**“写完一块就忍不住收尾”这个习惯**。

### 规则

```
chunks_per_turn = k
```

* 一个回合里链式跑满 k 块；**块与块之间不得收尾、不得汇报、不得提问**。
* 写完一块 → 直接拿下一块 → 全部跑完或 `gate` exit 0，才允许结尾。
* `plan --chunks k` 一次性给出连续 k 块的作业单，不用反复读状态。

### 它把数字变成什么

| 量 | 含义 |
|---|---|
| `x = rounds_planned` | 需要多少次模型输出（语义上的“轮”） |
| `k = chunks_per_turn` | 一个回合里链式跑几块 |
| `turns_needed ≈ ceil(x / k)` | **你实际需要接续的次数** |

例：目标 170,000、`cap=12000`、`util=0.8` → `x=18`；`k=3` → **需接续 6 次**（不链式是 18 次）。
`init` 会直接把这两个数字都打印出来。

> **诚实边界**：k 受 harness 的迭代上限约束，不是无限大；k 到底能到几取决于平台。
> 所以公式给的是“约”，但把 18 次压到 6 次是确定能拿到的。
> 另一个真边界：**x 本身（总输出量）不可压缩**——那是内容量，不是效率问题。

### 与“收尾”矛盾吗？

不矛盾。§1 的铁律 6 管的是**回合结束时**的结尾；本节管的是**回合进行中**的行为。
一句话：**中间不汇报，最后才汇报；但最后那一句仍必须是 `✅ DONE` 或 `⏩ RESUME`。**

---

## 2. 体量测算（第 1 步，必做）

```
单轮安全产出 P0 ≈ 9,000 中文字符（保守初值）
预估批次数 N0 = ceil(目标 / P0)      # 这只是初值
```

把 N0 直接告诉用户，并给分批计划表：

| 批次 | 范围 | 计划字数 | 累计 |
|---|---|---|---|
| 1 | 第1—40场 | 9,000 | 9,000 |
| 2 | 第41—80场 | 9,000 | 18,000 |

**第 1 批跑完立刻标定（v6 新增，关键）**：

```
实测字/块  = 本批新增字数 ÷ 本批新增块数
实测字/轮  = 本批新增字数
剩余轮数   = ceil(还差字数 ÷ 实测字/轮)
```

如果 `实测字/块` 明显低于预期（比如 < 800 字），**不要加快写块数，要加长每块**——否则你会写出几百个碎片块，既难看又不推进主线。

---

## 3. 骨架先行（Skeleton First）＋ 单写者锁

顺序不能反：

1. 先产出**全量骨架**：每个场/章只写"编号 + 一句锚点 + 计划字数"，不写正文。
2. 骨架一次交付确认（成本低、可改）。
3. 之后每批只做一件事：把骨架里的一段填成正文。
4. 填充时**不得改动骨架编号与顺序**（编号是续写的锚）。

**单写者锁（v6 新增）**：在项目根写 `RUN.lock`：

```json
{ "run_id": "2026-09-14T18:20", "file_prefixes": ["act4_"], "owner": "session-A" }
```

- 构建脚本**只扫描 `file_prefixes` 里的文件**，其余同前缀文件视为污染，直接报错；
- 如果发现同编号出现在不同文件里，**先失败，再人工决定留哪一份**，绝不自动合并。

---

## 4. 单元字数地板（v6 新增，反注水核心）

给每一类单元定地板：

| 单元 | 地板（中文字符） |
|---|---|
| 剧本·主场次 | ≥ 700 |
| 剧本·补充场 | ≥ 500 |
| 小说·章 | ≥ 2,000 |
| 报告·节 | ≥ 800 |

构建脚本对每个单元检查地板；**低于地板的单元列入"未达标清单"**，并算出：

```
需补字数 = Σ(地板 − 实际字数)
需补单元数 ≈ ceil(需补字数 ÷ 平均单元字数)
```

打印出**可以直接照着写的下一批清单**。这一步把"还差多少"从一句口号变成一张作业单。

---

## 5. 持久化账本

项目根放 `LEDGER.md`（人读）与 `ledger.json`（机读），每批结束必须更新。`ledger.json`：

```json
{
  "target": 170000, "unit": "cjk_chars",
  "calibrated": { "chars_per_block": 620, "chars_per_round": 9200 },
  "current_total": 106580, "remaining": 63420, "progress_pct": 62.7,
  "cursor": { "chapter": 12, "scene": 270 },
  "floors": { "main": 700, "supp": 500 },
  "checks": { "id_continuous": true, "time_monotonic": true, "fields_complete": true, "floors_ok": false }
}
```

`LEDGER.md` 必含四行：目标、当前、还差、完成度，以及"下一批范围 + 计划字数"。

---

## 6. 单批标准作业流程（SOP）

```
1. 读 LEDGER.md → 取 cursor 与下一批范围
2. 只写这一批（目标 = 实测字/轮，宁少勿超）
3. 落盘（文件名带序号，便于排序；遵循 RUN.lock 前缀）
4. 运行 build_and_verify.py
5. 字数不够 → 立刻在本批内补写，不许把差额留到下一批
6. 更新 ledger.json / LEDGER.md
7. 汇报（格式见 §8）
8. 若用户要求"全部写完" → 回到步骤 1，继续下一批，直到本轮上限
```

**不要**在同一批里回头改已验收的历史章节——那会让校验基线失效。

---

## 7. 验收闸门（每批必跑）

| 闸门 | 判据 | 失败处理 |
|---|---|---|
| G1 字数 | `current_total` 单调增长且 ≥ 本批承诺 | 立刻补写 |
| G2 编号连续 | 主编号无跳号、无重号 | 重排＋全局回写 |
| G3 时间单调 | 时间戳严格递增，零倒流 | 调整时间戳 |
| G4 字段齐全 | 每单元必填字段全在 | 补齐 |
| G5 结构完整 | 集/卷/章标题齐全 | 补标 |
| G6 退化扫描 | 无"主语，动词"碎片、无连续空行 ≥3 | 改写 |
| G7 交付可打开 | docx/pdf 能打开 | 重生成 |
| **G8 重号污染** | 同编号不出现在两个文件；`file_prefixes` 外文件不得被扫描 | **失败即停，人工裁决** |
| **G9 单元地板** | 每单元字数 ≥ 地板 | 输出补写清单 |
| **G10 内容重复/事实冲突** | ①无逐字重复段落 ②同场各块事实不打架（人数/道具/来因） ③补充场不得换词重写主场次事件 ④正文日期不与时间字段脱节 | **失败即停：改写或删除该块，再重建** |
| **P0 停止谓词** | `run_state.py gate` 退出码：`0=DONE / 1=RUNNING / 2=BLOCKED` | **非 0 即不许停、不许写"完成"**（v8 新增，优先级高于一切） |

---

## 7.5 闸门 G10 详解（v7 新增，跨来源拼接必跑）

### 为什么需要它

v6 的 G1–G9 全部是**形式**判据（字数、编号、时间、字段、结构、退化、可打开、重号、地板）。
它们有一个共同盲点：**一份"看起来完全合规"的稿子，内容可以是互相打架的。**

真实事故（本项目实测）：底稿是 A 路运行写好的，B 路后来往同一场次下面追加了一条补充场：

- 主场次写的是"安撞门救人"；新补充场把**同一件事换了一套词又写了一遍**，时间戳却标成第二天中午；
- 新补充场说"小晚是跟他约好吃饭"，而主场次与另一条补充场说"小晚是跟妈妈来旅游、在街上偶遇"；
- 到场人数一处写三人、一处写四人；绑具一处写布条、一处写胶带。

**编号、时间、字段、字数全对，G2~G9 一个都没拦住。**

### 四路判据

| 编号 | 判据 | 说明 |
|---|---|---|
| G10a | **逐字重复** | 任意两块之间存在 ≥30 字完全相同的段落，且占较短块 ≥6% |
| G10b | **同场事实冲突** | 同一主场次下各块之间：互斥要素组（绑具=胶带/布条/绳/扎带/手铐；封门；载具）取值不相交；或"个/名/人"数量取值不同 |
| G10c | **疑似换词重写** | 时间晚于主场次的补充场，与其主场次共享 ≥3 个"收束动作"动词（踹开/逮住/撕开/绑/拽开 …） |
| G10d | **正文日期错位** | 正文提到的日期比"时间："字段早 30 天以上，且不含"那年/当时/第一次/生日"类引用词 |

另出 **逐场对照表**（`--cross-table`）：把同一场次各块的 时间/地点/出场人物/量词/道具 并排列出。
**语义级重复（同一事件换词重写）字面相似度很低，自动化只能给线索，最终裁定必须靠人看这张表。**

### 变现场景：跨来源接管前置检查

当你接手一份"别人写过 / 更早批次写过"的底稿时，**动笔前**依次做三步：

1. 跑 `python scripts/dedupe_scan.py <src> --cross-table --out g10.txt`，把 g10.txt 通读一遍；
2. 把要新增的每一个补充场在对照表里**对位检查**：时间在主场次之后，就绝不能再写主场次已经写完的动作；
3. 涉及同一事件的细节（几个人、什么工具、什么来因）**一律沿用底稿已确立的那个版本**，不得另起一套。

### 命令

```bash
python scripts/dedupe_scan.py scenes --cross-table --out g10.txt
# 退出码：0=无必修复项；1=存在必须修复项
```

---

## 8. 汇报格式（固定）

```
## 第 N 批已交付
| | 上轮 | 本轮 | 增量 |
|---|---|---|---|
| 上册 | … | … | … |
| 下册 | … | … | … |
| 合计 | … | … | … |

目标 X ｜ 当前 Y ｜ 还差 Z（完成度 P%）
实测吞吐：字/块 = a ｜ 字/轮 = b ｜ 预计还需 r 轮

新增：<一段话说清这批写了什么>
校验：G1✓ G2✓ … G9✓

下一批：<范围 + 计划字数>
```

这张表让用户**永远不必自己发现"又不够"**。

---

## 9. 断点续写（Resume）

用户只说"继续"时：

1. 读 `LEDGER.md` / `ledger.json`；
2. 取 `cursor`，用**实测吞吐**算下一批范围与轮数；
3. 按 §6 执行（若用户此前说过"全部写完"，继续链式跑批）；
4. **不要重新问需求**，不要重述背景。

---

## 10. 反模式（见到就停手）

- ❌ 先答应"一次写完"，然后被截断
- ❌ 把几万字正文贴在聊天里
- ❌ 用短"补充场/附录"堆字数而主线不推进（v6 用 G9 拦截）
- ❌ 跳过某批校验，凑到最后一起查
- ❌ 进度只存在对话里，换会话就归零
- ❌ 明明差一大截却先汇报"已完成"
- ❌ 用"每轮 10,000 字"的估值排期，从不回测（v6 强制标定）
- ❌ 多轮/并行运行共用同一目录却不加锁（会写出重号稿）
- ❌ **往别人写好的底稿里追加补充场，却不先跑 G10**（编号全对，内容却重写／打架）
- ❌ **同一事件在不同块里各立一套细节**（三人/四人、布条/胶带、偶遇/约好）——G10b 会当场拦下

---

## 11. 配套脚本

| 脚本 | 作用 |
|---|---|
| `scripts/run_state.py` | **【v9 核心】轮数契约 + 四阶段 + 停止谓词 + 轮末契约 + 吞吐升级梯**：`init / next / plan / count / tick / gate / report / resume / where` |
| `scripts/ledger.py` | 账本初始化/更新/查询/算下一批 |
| `scripts/build_and_verify.py` | 重建＋G2–G9 全部校验＋输出**下一批作业单** |
| `scripts/size_report.py` | 一行命令回答"目标/当前/还差/完成度/还需几轮" |
| `scripts/dedupe_scan.py` | **G10**：内容重复/事实冲突/跨来源拼接冲突扫描＋逐场对照表（`--cross-table`） |
| `scripts/publish_github.py` | 把技能目录整体推送到 GitHub 仓库（UTF-8 安全） |

用法见 `docs/workflow.zh.md` / `docs/workflow.en.md`。

> **v7 起：跨来源拼接前，必须先跑 G10**（`dedupe_scan.py --cross-table`）。
> 形式闸门（G1–G9）保证"长得对"，G10 保证"说得不矛盾"。

---

## 12. 排障表（症状 → 原因 → 动作）

| 症状 | 原因 | 动作 |
|---|---|---|
| "写了几轮，发现只完成 30%" | 用估值排期，未标定 | 跑 `size_report.py`，用实测吞吐重排 |
| "块数很多但总字数上不去" | 单块太短（注水式补充场） | 看 G9 未达标清单，**加长单块**而不是加块数 |
| "两轮之间编号对不上/出现重复场次" | 多写者/多轮共用目录 | 建 `RUN.lock`；G8 失败即停，人工裁决 |
| "换了个会话就全部重来" | 进度只在对话里 | 账本必须落盘（§5） |
| "每轮都要重新说一遍要求" | 需求没写进骨架 | 骨架先行（§3），需求进骨架不进对话 |
| "最后才发现差一大截" | 没有固定汇报表 | 用 §8 格式，每批强制 `还差 Z` |
| "编号时间都对，可读起来同一件事写了两遍" | 往别人底稿追加补充场，只过了形式闸门 | 跑 `dedupe_scan.py`（G10）；补充场换角度重写 |
| "同一场里前后说法不一样（几个人/什么工具）" | 不同来源的块各立一套细节 | G10b 列为必修复；以底稿已确立的版本为准统一 |
| "G10 报了很多条，要不要全改" | G10b/G10c 是启发式，存在合理误报 | 先看 ERROR，再看 WARN；**逐场对照表**为人裁提供依据 |
| "写了几轮就被当成写完了" | **没有停止谓词**，"完成"是自评 | 跑 `run_state.py gate`，看退出码；非 0 不许收工（§1.5.2） |
| "结尾又在问我'要不要…'" | 轮末没有契约 | 结尾只许 `✅ DONE` 或 `⏩ RESUME`（§1.5.3） |
| "每轮都要我手动催'继续'" | 续跑责任被推给了用户 | 跑 `run_state.py resume` 拿一行指令；agent 应主动接续，用户只说"继续" |
| "实测吞吐很低，下一轮还是老样子" | 只报告，没强制 | `tick` 自动执行吞吐升级梯（§1.5.4），块数下降、单块变长 |
| "不知道该跑几轮" | 没有轮数契约 | `init --cap --util` 先算出 x，写进 `rounds_planned`（§1.6.1） |
| "字数怎么算才算数" | 单一口径可自欺 | `count` 给五种口径；`gate` 取**最严**口径（§1.6.3） |
| "跑满 x 轮了是不是就完成了" | 混淆排期与验收 | **不是**。x 只是排期；只有 `gate` exit 0 才算完成 |
| "字数够了但有毛病，能不能先交" | 没有阶段划分 | `REPAIR` 阶段 exit 2：修完重跑，不许交 |

---

## 13. 更新日志

### v9.1.0
- **新增轮内链式续跑**：`init --chunks-per-turn k`（默认 3）；`plan --chunks k` 一次给出连续 k 块作业单。
  铁律 8：**块与块之间不得收尾、不得汇报、不得提问**。
- `init` 直接打印**“需你接续的次数” ≈ ceil(x / k)**：17 万字从“催 18 次”降到“催 6 次”。
- 新增 §1.7，把“回合内可含多次模型输出”这个机制写成规则；并明确 k 受 harness 迭代上限约束。
- 诚实边界：**x（总输出量）不可压缩**，能压缩的只是接续次数。
- 回归测试扩到 **28 项断言**（含链式作业单与接续次数计算）。

### v9.0.0
- **新增轮数契约**：`init --cap --util` 先算出 `x = ceil(目标 ÷ (上限×0.8))`，写进 `rounds_planned`，
  每轮显示 `轮 n/x`。**先算排期，再开写**。
- **新增四阶段循环**：`GENERATE → AUDIT → REPAIR → DONE`，完全对应
  “跑满 x 轮 → 多路核字数 → 不够就补 → 够了再排查 → 全过才停”。
- **新增多路字数核算** `count`：汉字／含全角标点／去空白／总字符／**交付 docx** 五种口径；
  `gate` 判定时取**最严口径** `min(源文件, docx)`，防止“源文件够但 docx 漏内容”被误判达标。
- **新增 `next`**：一句话告诉你“现在该干什么”（继续写 / 先修问题 / 允许停），并给对应退出码。
- 回归测试扩到 **24 项断言**，覆盖轮数计算、四阶段流转、五种口径与全部负向用例。

### v8.0.1
- **修复（重要）**：`description` 曾被撑到 1341 字符，超过 Chatbox `skills:parser` 的 **1024 字符上限**，
  导致技能解析失败、被摘出启用列表（现象：**“技能不见了”**）。已压缩到 743 字符。
  自查命令见 `_selftest/fix_desc.py`；恢复启用项见 `_selftest/restore_enabled.py`。
  **写 description 时务必控制在 1024 字符以内（留余量，≤900 更安全）。**

### v8.0.0
- **新增停止谓词（M1）**：`scripts/run_state.py gate`，退出码 `0=DONE / 1=RUNNING / 2=BLOCKED`。
  **"完成"从主观判断变成可验证事实**；退出码非 0 一律不许停。
- **新增轮末契约（M2）**：每轮回复只许以 `✅ DONE` 或 `⏩ RESUME` 结尾；
  明令禁止"要不要…""如果你希望…""回我一句…"等一切变相停下提问。
- **新增吞吐升级梯（M3）**：`cpu = max(地板 × 1.4, 上轮 cpu × 1.35)`，`units = turn_budget ÷ cpu`。
  用算术强制"加长单块"，并含 STALL 停滞检测。
- BLOCKED 分支也必须输出 RESUME——修掉"修完问题后就地停下"的隐性漏洞。
- 新增 `_selftest/selftest.py`：12 项断言，覆盖 RUNNING / DONE / BLOCKED 三种退出码，
  以及重号、时间倒流、G10 拦截四条负向用例。
- 新增 `templates/turn_contract.md`：可直接粘进项目根的轮末契约卡片。
- 新增 `run_state.py where / report`：换会话时一屏看清当前口径。

### v7.0.0
- **新增闸门 G10：内容重复 / 事实冲突 / 跨来源拼接冲突**（v6 的形式闸门挡不住的那一类）。
- 新增 `scripts/dedupe_scan.py`，含四路判据（G10a 逐字重复 / G10b 同场事实冲突 /
  G10c 疑似换词重写 / G10d 正文日期错位）与 **`--cross-table` 逐场对照表**。
- 新增 **§7.5 跨来源接管前置检查**：接手别人底稿时，动笔前必须先跑 G10 并逐场对位。
- 排障表新增 3 行；反模式新增 2 条。
- 阈值经实测标定：逐字重复 30 字＋6% 覆盖度；互斥要素组；收束动作 ≥3。
- 已知局限：语义级"换词重写"无法纯自动识别，G10c 只给线索，最终靠对照表人工裁定。

### v6.0.0
- 吞吐标定；G9 单元字数地板＋自动作业单；G8 重号/污染检测＋RUN.lock；publish_github.py；第 4 条铁律。

### v5 及更早
- 分批生成、骨架先行、账本、退化检测与场次/时间线校验的雏形。

---

## 14. 参考

基于 [Wikipedia:Signs of AI writing](https://en.wikipedia.org/wiki/Signs_of_AI_writing) 的思路，本协议关注的是**工程可控性**而非文风。

v8 的核心思想来自一条工程常识：**凡是不能被机器验证的"完成"，都不算完成。**
所以停止谓词不写在提示词里，而写进 `run_state.py` 的退出码里。
